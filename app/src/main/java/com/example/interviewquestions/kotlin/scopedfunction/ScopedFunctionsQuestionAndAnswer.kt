package com.example.interviewquestions.kotlin.scopedfunction

//(1) How Many types of scoped functions?

//Ans: There are 5 types of scoped functions

//(1) let (2) run (3) with (4) apply (5) also

//(2) apply and also return only object and others return lambda result it can be any thing these return objects also

//(3) Context Of Object: let and also return it and with, run and apply return this

//(4) Context Renaming: We can rename it but cannot rename this

//(5) Easy Null Check: In with we cannot apply null check only else we can